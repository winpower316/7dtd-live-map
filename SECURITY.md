# セキュリティ

## 脆弱性の報告

公開Issueへ認証情報、IPアドレス、プレイヤー情報、ログを貼らないでください。GitHubのSecurity Advisoryから非公開で報告してください。

## 運用上の前提

- `secrets/`、`.env`、SQLite、監査ログをGitへ追加しない。
- 7DTDの標準ダッシュボード、セッションAPI、汎用コマンドAPIをインターネットへ中継しない。
- Windowsエージェント用ポートはLAN内でも送信元を制限する。
- `RESTART_ENABLED=false`を既定のまま使い、必要な環境だけ明示的に有効化する。
- 公開前にGitHub ActionsのGitleaks結果を確認する。
- プレイヤー位置、寝袋、クエスト、共有地点、履歴は個人情報になり得るため、参加者の同意を得て運用する。

侵害が疑われる場合は、合い言葉、7DTD APIトークン、エージェントトークンをすべてローテーションしてください。
